defmodule Challenge do
  def get_middle(str) do 
    # return the middle charater(s)
    l = String.length(str)
    h = div(l,2)
    if h == l/2 do
        String.at(str, h-1) <> String.at(str, h)
    else
        String.at(str, h)
    end
  end
end
